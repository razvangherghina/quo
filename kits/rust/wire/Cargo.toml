[package]
name = "quo-wire"
version = "0.0.0"
description = "The Quo wire encoding: the one way each closed type is written, and the bytes a decoder refuses."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
homepage = "https://quo.systems"
repository = "https://github.com/razvangherghina/quo"
keywords = ["quo", "protocol", "encoding", "serialization"]
categories = ["encoding", "cryptography"]

[dependencies]
quo-notation = { version = "0.0.0", path = "../notation" }
