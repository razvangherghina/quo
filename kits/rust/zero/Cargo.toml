[package]
name = "quo-zero"
version = "0.0.0"
description = "Distance zero: the Quo road with no wire — two houses in one process handing envelope bytes as bytes, judged in full."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
homepage = "https://quo.systems"
repository = "https://github.com/razvangherghina/quo"
keywords = ["quo", "protocol", "transport", "in-process"]
categories = ["network-programming"]

[dependencies]

[dev-dependencies]
quo-arithmetic = { path = "../arithmetic" }
quo-carriage = { path = "../carriage" }
quo-envelope = { path = "../envelope" }
quo-line = { path = "../line" }
quo-notation = { path = "../notation" }
quo-warden = { path = "../warden" }
quo-wire = { path = "../wire" }
